# Emby 元数据管理工具任务清单

> 已与当前架构同步（2026-07-28）。原方案设想的 Flask + 独立 `backend/` 目录已废弃，改为复用现有 FastAPI（`bot/api/`）+ 服务层（`bot/services/`）。
> 可执行任务以 [`docs/治理待办清单.md`](../治理待办清单.md) 的 Part B 为唯一真相源，本文只做分阶段说明。

状态说明：`[ ]` 未开始 / `[-]` 进行中 / `[x]` 已完成。

## A. 前置确认

- [ ] 确认 Emby 版本和 API 权限。
- [ ] 确认 Emby 公网访问地址、HTTPS 和访问控制方式。
- [ ] 确认计划使用的数据源及其授权、robots、频率限制和登录要求。
- [ ] 确认第一阶段只处理电影，不处理剧集层级。
- [ ] 确认元数据覆盖策略：仅填空字段或允许覆盖。

## B. 基础工程（复用现有）

- [x] 复用现有 Python 项目和依赖管理（`pyproject.toml`）。
- [x] 复用现有配置结构（`bot/core/config.py` 的 `settings`，从根 `.env` 加载）。
- [x] 复用现有 FastAPI 应用（`bot/api/app.py`），不新建 Flask。
- [x] 复用现有统一日志（`loguru`）。
- [ ] 确认 API 默认仅监听 `127.0.0.1`（或通过反代/访问控制保护）。

## C. Emby 接入（补全 EmbyClient）

- [x] 复用现有 Emby API 客户端（`bot/core/emby.py` 的 `EmbyClient`）。
- [x] 补 `get_system_info()`：连接测试 `GET /System/Info`。
- [x] 复用 `get_users()`：用户或管理员 ID 获取。
- [x] 复用 `get_items(ids, user_id=...)`：电影列表读取。
- [x] 复用 `get_item(user_id, item_id)`：完整条目读取。
- [x] 补 `update_item(item_id, item_data)`：字段级元数据更新 `POST /Items/{ItemId}`。
- [x] 补 `upload_item_image(item_id, image_data, image_type)`：主封面上传 `POST /Items/{ItemId}/Images/{Type}`。
- [ ] 验证 API Key 无效、权限不足和服务器不可达时的提示。

## D. 数据源接入（`bot/services/emby_metadata/sources/`）

- [x] 定义统一的 `MetadataCandidate` 内部模型（pydantic），包含国产/日韩/欧美分类。
- [x] 定义数据源适配器接口（抽象基类）。
- [x] 实现首个日韩公开数据源 `ck_download` 适配器。
- [x] 实现搜索结果解析、去重和候选置信度排序。
- [x] 实现详情页字段解析。
- [ ] 实现封面下载（公开页无明确主图时不猜测、不下载）。
- [-] 增加请求超时、重试上限和访问间隔（已完成 15 秒总超时；重试和访问间隔待后续策略确认）。
- [x] 对页面结构变更返回明确错误。
- [ ] 在正式使用前确认目标站点条款和授权边界。

## E. 匹配与预览（`bot/services/emby_metadata/matching.py`）

- [ ] 实现文件名清理：分辨率、编码、组名等噪声移除。
- [-] 支持标题、编号和手动关键词搜索（已完成“番号 标题”提取和关键词搜索；手动关键词由 HTTP/Web 阶段接入）。
- [ ] 支持标题加年份匹配。
- [x] 对候选结果计算简单置信度，番号精确匹配优先。
- [x] 多候选时按置信度排序。
- [ ] 展示 Emby 当前元数据与候选元数据的对比。
- [ ] 支持逐字段勾选。
- [ ] 已有字段覆盖前显示警告。

## F. HTTP 路由（`bot/api/routes/emby_metadata.py`）

- [ ] 实现 `GET /api/emby/metadata/system-info` 连接测试。
- [ ] 实现 `GET /api/emby/metadata/movies` 电影列表（分页）。
- [ ] 实现 `GET /api/emby/metadata/movies/{item_id}` 完整条目。
- [ ] 实现 `POST /api/emby/metadata/search` 候选搜索。
- [ ] 实现 `POST /api/emby/metadata/update/{item_id}` 字段级更新（带确认参数，不能仅 GET 触发写入）。
- [ ] 在 `bot/api/app.py` 注册新 router。
- [ ] 统一错误响应格式。

## G. Web UI（`web/src/`，后续阶段）

- [ ] 实现连接配置区域。
- [ ] 实现电影列表区域。
- [ ] 实现数据源选择。
- [ ] 实现关键词输入和搜索。
- [ ] 实现候选结果展示。
- [ ] 实现元数据预览和字段选择。
- [ ] 实现更新确认。
- [ ] 实现加载、空结果、成功和失败状态。
- [ ] 实现处理历史或最近操作记录展示。

## H. 测试与验证

- [ ] 测试正常唯一匹配。
- [ ] 测试标题带年份。
- [ ] 测试文件名带技术信息。
- [ ] 测试空标题。
- [ ] 测试无搜索结果。
- [ ] 测试多候选结果。
- [ ] 测试已有元数据不被默认覆盖。
- [ ] 测试按选择覆盖字段。
- [ ] 测试 API Key 错误。
- [ ] 测试图片下载失败和 Emby 更新失败。
- [ ] 对真实电影先执行单条更新，再验证批量操作。

## I. 发布与运维

- [ ] 编写 Windows 启动方式（复用 `run_all.py`）。
- [ ] 增加配置备份说明。
- [ ] 增加日志保留策略。
- [ ] 增加公网部署安全检查。
- [ ] 记录数据源变更和适配器维护事项。

## 当前进度

- [x] 完成需求澄清：Web UI、电影为主、Emby 可公网访问。
- [x] 完成目标网站基础结构调研。
- [x] 生成整体设计方案：见 `docs/gv-data-capture/设计方案.md`。
- [x] 生成任务清单：本文档。
- [x] 架构确认：复用现有 FastAPI（`bot/api/`）+ 服务层，废弃 Flask + 独立 backend 方案。
- [x] 复用现有配置：`bot/core/config.py` 的 `settings` + 根 `.env`。
- [x] 补全 EmbyClient 元数据更新能力（B1 阶段）：get_system_info / update_item / upload_item_image。
- [ ] 填写真实 Emby 地址和 API Key 后执行接口联调。
