# 🌸 桜色男孩 (LustfulBoy) - 经济系统设计

> **状态**: 📝 设计中
> **日期**: 2025-12-24
> **官网**: lustfulboy.com
> **适用**: Telegram Bot (Aiogram 3.x)
> **主题**: BL / Gay / Emby / 社交

## 1. 核心概念 (Core Concepts)

采用积分制激励用户活跃。

*   **代币名称**: **精粹 (Essence)** (代码中需定义为全局常量 `CURRENCY_NAME` 以便修改)
*   **代币符号**: `💧` (代码中需定义为全局常量 `CURRENCY_SYMBOL`)
*   **核心逻辑**: 
    *   用户通过活跃（签到、聊天、互动）获取代币。
    *   积攒精粹可以换取 Emby 观影特权或群组特殊身份。

## 2. 交互体验设计 (UX Design)

✅ **采用**: 全 **UI/按钮** 驱动，无缝集成在用户面板中。

### A. 每日签到 (Daily Check-in)
*   **入口**: 在主面板显示 `[📅 每日签到]` 按钮。和精粹商店按钮在一行，精粹商店在左边
*   **状态显示**: 
    *   未签到时：按钮亮起，文案为 `📅 每日签到`。
    *   已签到时：按钮变灰或文案变为 `✅ 今日已签`，并在面板下方显示 `💧 精粹: 120`。
*   **交互流程**:
    1.  用户点击 `[📅 每日签到]`。
    2.  Bot 弹窗 (Alert) 提示：
        > "签到成功！
        > 获得: +20 💧
        > 连续: 3 天 (加成 +10%)
        >
        > 💬 今日运势: 宜面基，忌独守空房。"
    3.  Bot **静默刷新** 原消息，更新余额显示，不发送新消息刷屏。

### B. 精粹商店 (Essence Store)
*   **入口**: `[🛍️ 精粹商店]` 按钮。
*   **界面**: 使用内联键盘翻页展示可兑换商品。
*   **商品**:
    *   **补签卡**: 80 💧 = 恢复断签。
    *   **图像修改**: 30 💧 = 修改 Emby 头像/背景 (待定)。
    *   **密码修改**: 60 💧 = 修改 Emby 头像/背景 (待定)。
    *   **自定义头衔**: 100 💧 = 自定义群组 Title (如 "🌸 樱之主") ⏳ 限时版 100 💧 / 7 天 ♾️ 永久版 500 💧 连签 ≥ 30 天，库存10

## 3. 数据库设计 (Schema Optimization)

使用通用的命名规范，便于维护和扩展。

> **注意**: 所有数据表模型均需继承 `BasicAuditMixin`，自动包含以下审计字段：
> *   `created_at` (创建时间)
> *   `updated_at` (更新时间)
> *   `created_by` (创建者ID)
> *   `updated_by` (更新者ID)
> *   `is_deleted` (软删除标记)
> *   `deleted_at` (删除时间)
> *   `deleted_by` (删除者ID)
> *   `remark` (备注)

### 1. `user_extend` (用户扩展表)
复用已有的 `user_extend` 表，新增经济系统相关字段。

| 字段名 | 类型 | 属性 | 说明 |
| :--- | :--- | :--- | :--- |
| `user_id` | BigInt | PK, FK | 关联 `users.id` |
| ... | ... | ... | (原有字段: role, emby_user_id, bio 等) |
| `currency_balance` | Integer | Default 0 | 当前代币余额 |
| `currency_total` | BigInt | Default 0 | 历史累计获取总量 (用于等级/成就) |
| `streak_days` | Integer | Default 0 | 当前连续签到天数 |
| `last_checkin_date` | Date | Nullable | 上次签到日期 (用于计算断签) |
| `...` | ... | ... | **BasicAuditMixin 字段** |

### 2. `currency_transactions` (流水表)
不可变日志，用于审计和回溯。

| 字段名 | 类型 | 属性 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BigInt | PK, Auto | 流水ID |
| `user_id` | BigInt | FK, Index | 关联 `users.id` |
| `amount` | Integer | Not Null | 变动数值 (正数为获取，负数为消耗) |
| `balance_after` | Integer | Not Null | **变动后余额** (快照，防止计算误差) |
| `event_type` | Varchar(32)| Index | 事件类型: `daily_checkin`, `redeem_emby` 等 |
| `description` | Varchar(255) | Nullable | **流水描述**: 人类可读的说明 (如 "购买 Emby 30天") |
| `meta` | JSON | Nullable | 扩展信息 (如: `{"streak": 5, "product_id": 1}`) |
| `...` | ... | ... | **BasicAuditMixin 字段** |

## 4. 动态配置与商品系统 (Dynamic Configuration & Products)

为了支持管理员动态调整数值和管理商品，我们将规则与商品信息存入数据库。

### 1. `currency_config` (配置表)
存储各种行为的奖惩数值，支持动态调整。

| 字段名 | 类型 | 属性 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | Integer | PK, Auto | 配置ID |
| `config_key` | Varchar(64) | Unique | 配置键名 (如 `checkin_base`, `checkin_bonus_rate`) |
| `value` | Integer | Not Null | 数值 (如 20, 10) |
| `description` | Varchar(255) | Nullable | 配置说明 |
| `...` | ... | ... | **BasicAuditMixin 字段** |

注意配置键名规则：
{domain}.{action}.{type}
例如：
checkin.base —— 每日签到基础奖励（10 💧）
checkin.streak_bonus_rate —— 连签加成百分比（5%/天）
checkin.random_bonus —— 随机浮动奖励（比如 0~5）
checkin.weekly_bonus —— 连签 7 天额外奖励（20 💧）
checkin.monthly_bonus —— 连签 30 天大礼包（50 💧）



### 2. `currency_products` (商品表)
存储商店可售卖的商品信息，支持上下架、有效期管理以及**双重权限控制**。

| 字段名 | 类型 | 属性 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | Integer | PK, Auto | 商品ID |
| `name` | Varchar(128) | Not Null | 商品名称 (如 "Emby 1天权限") |
| `description` | Text | Nullable | 商品描述 |
| `price` | Integer | Not Null | 价格 (代币) |
| `category` | Varchar(32) | Index | 分类: `tools`, `emby`, `group` |
| `action_type` | Varchar(32) | Index | 行为类型: `retro_checkin`, `emby_image`, `custom_title` |
| `reward_value` | JSON | Nullable | 实际效果参数 (如 `{"days": 1}`, `{"title": "🌸"}`) |
| `stock` | Integer | Default -1 | 库存 (-1表示无限) |
| `visible_conditions` | JSON | Nullable | **可见条件**: 谁可以看到该商品 (如 `{"has_emby": true}`) |
| `purchase_conditions`| JSON | Nullable | **购买条件**: 谁可以购买该商品 (如 `{"min_days": 30}`) |
| `start_time` | DateTime | Nullable | 上架时间 (空表示立即) |
| `end_time` | DateTime | Nullable | 下架时间 (空表示永久) |
| `is_active` | Boolean | Default True | 是否上架 |
| `...` | ... | ... | **BasicAuditMixin 字段** |

## 5. 签到算法 (Algorithm)

*   **基础奖励**: 10 ~ 20 💧 (随机浮动，增加趣味性)。
*   **连签加成**: 
    *   `bonus = base * min(streak * 0.1, 1.0)`
    *   上限为 100% 加成 (即连签10天达到双倍)。
*   **断签逻辑**: 
    *   若 `today - last_checkin_date > 1`，则 `streak_days` 重置为 1。
*   **历史记录**:
    *   每次签到后，若 `streak_days > max_streak_days`，则更新 `max_streak_days`。

## 5. 开发计划 (Roadmap)

1.  **Phase 1**: 创建数据库表，实现 `CurrencyService` (增删查改)。
2.  **Phase 2**: 修改 `get_account_center_keyboard`，加入签到按钮。
3.  **Phase 3**: 实现签到回调逻辑 (`checkin_callback`) 与弹窗反馈。
4.  **Phase 4**: 开发商店界面与兑换逻辑。
