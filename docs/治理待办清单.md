# 项目治理待办清单

> 由 `docs/project-structure-review.md`（结构分析）与 `docs/gv-data-capture/`（功能设计）汇总而来。
> 勾选规则：`[ ]` 未开始 / `[-]` 进行中 / `[x]` 已完成。
> 优先级：**P0** 必须治理 · **P1** 建议治理 · **P2** 可选治理。

---

## Part A：结构与命名治理

来源：`docs/project-structure-review.md`。落地顺序按"低风险 → 高风险"排。

### A1. 清理冗余（低风险，可立即做）

- [ ] **A1.1 [P0]** 删除孤儿 migration 目录 `bot/database/migrations/`（无 env.py、零引用）。
- [x] **A1.2 [P2]** 根 `assets/` 已整体删除：6 字体重复已删、`banner copy.txt` 已删；`banner.txt`/`ui/start.jpg`/`sakura.png` 归位到 `bot/assets/` 对应位置，`banner.py` 路径改为基于 `__file__`。
- [ ] **A1.3 [P1]** 清理根目录散落文件：
  - [ ] `test_emby_api.py`（引用不存在的 `emby_client.close()`）→ 删除或移 `bot/tests/` 并修复。
  - [ ] `test_series_notification.py`（引用不存在的 `get_series_info`）→ 删除或移 `bot/tests/` 并修复。
  - [ ] `emby_test_commands.md`（引用不存在的 `EMBY_ADMIN_ID`、`test_emby_quick.py`）→ 移 `docs/` 或删除。
  - [ ] `run_emby_device_sync.py` → 移 `scripts/`。
  - [ ] `run_all.py` → 保留根（统一入口）或移 `scripts/`。

### A2. 配置层重命名（中风险，需全局替换 import）

- [ ] **A2.1 [P1]** `bot/core/constants.py` → `bot/core/business_constants.py`（业务值常量，区别于 DB 键名）。
- [ ] **A2.2 [P1]** `bot/config/` → `bot/features/`（语义：DB 功能开关），避免与 `core/` 混淆。
- [ ] **A2.3 [P1]** 去掉 `bot/config/__init__.py` 的 `*` re-export，改显式 import。

### A3. Emby 重构（高风险，建议单独立项）

- [ ] **A3.1 [P0]** 拆分 `bot/services/emby_service.py`（1187 行上帝模块）：
  - [ ] `services/emby/user_sync.py`（用户同步 `save_all_emby_users`）
  - [ ] `services/emby/device_sync.py`（设备同步 `save_all_emby_devices`）
  - [ ] `services/emby/policy.py`（Policy 清理 `cleanup_devices_by_policy`）
  - [ ] `services/emby/history.py`（设备历史快照 `build_device_snapshot` 等）
  - [ ] `services/emby/scheduler.py`（调度器 `start_scheduler`）
- [ ] **A3.2 [P0]** 删除重复 `_canon_json`（`emby_service.py` 与 `emby_update_helper.py` 各一份），统一到一处。
- [ ] **A3.3 [P0]** scripts 里复制的清理逻辑改为调用 service 函数（`scripts/sync_emby_config.py`）。
- [ ] **A3.4 [P0]** 硬编码用户 ID 外置到 `.env` 或 CLI 参数：
  - [ ] `scripts/sync_emby_users_from_template.py`（39 个真实用户 ID）
  - [ ] `scripts/sync_emby_config.py`（`exclude_ids`）
  - [ ] `scripts/update_emby.py`（`exclude_ids`）
- [ ] **A3.5 [P1]** `bot/tools/emby_template_sync.py` 移到 `services/emby/`（运行期批量操作，非开发工具）。

### A4. handlers 治理（中风险）

- [ ] **A4.1 [P0]** 删除 `bot/handlers/command/test/` 目录 + `bot/handlers/command/__init__.py` 中的硬编码 include；若确需保留测试命令，用 `if settings.debug` 包裹并加 `__init__.py`。
- [ ] **A4.2 [P1]** handlers 跨层同名加后缀：
  - [ ] `handlers/admin/notification/submission_review.py` → `submission_review_ui.py`
  - [ ] `handlers/command/admin/submission_review.py` → `submission_review_cmd.py`
  - 约定：UI 侧 `_ui`、命令侧 `_cmd`。
- [ ] **A4.3 [P2]** `handlers/command/admin/quiz.py`/`quizs.py` → `quiz_single.py`/`quiz_batch.py`。

### A5. 细节一致性

- [ ] **A5.1 [P2]** `bot/states/` 补 `__init__.py`，风格统一；明确状态集中定义 vs 本地定义的边界。
- [ ] **A5.2 [P2]** 删除/重写 `bot/tools/generate_feature.py`（生成代码引用不存在的 `bot.features` 包）。
- [ ] **A5.3 [P2]** `bot/handlers/user/submission.py` → `submission_menu.py`（与其余 `submission_*.py` 对称）。

---

## Part B：gv-data-capture 功能开发

来源：`docs/gv-data-capture/设计方案.md`。已按当前实际架构（FastAPI 复用）调整，不再用 Flask + 独立 backend。

### B0. 前置（已完成）

- [x] **B0.1** 架构确认：复用现有 `bot/api/`（FastAPI）+ `bot/services/`，不另起 Flask。
- [x] **B0.2** 同步初期文档（Flask → FastAPI、backend → bot/api+services）。

### B1. EmbyClient 元数据能力补全

- [x] **B1.1 [P0]** `bot/core/emby.py` 补 `get_system_info()`：测试连接 `GET /System/Info`。
- [x] **B1.2 [P0]** `bot/core/emby.py` 补 `update_item(item_id, item_data)`：更新元数据 `POST /Items/{ItemId}`。
- [x] **B1.3 [P0]** `bot/core/emby.py` 补 `upload_item_image(item_id, image_data, image_type)`：上传 Item 封面 `POST /Items/{ItemId}/Images/{Type}`（区别于已有的 `upload_user_image`）。
- [x] **B1.4 [P1]** 真实 Emby 联调：✅ `get_item` 读完整条目、✅ `update_item` 写回（可逆测试通过）；✅ `upload_item_image` 待图片测试。

### B2. 抓取业务层骨架（bot/services/emby_metadata/）

- [ ] **B2.1 [P0]** 定义内部模型 `MetadataCandidate`（pydantic）：source/source_id/title/overview/year/genres/studios/poster_url/confidence 等。
- [ ] **B2.2 [P0]** 定义数据源适配器接口（抽象基类）：`search(keyword)` / `fetch_detail(source_id)`。
- [ ] **B2.3 [P1]** 实现第一个数据源适配器（`sources/hunk_ch.py`，基于已调研的 `search.php?s=` / `movie_detail.php?code=`）。
  - 正式使用前需确认授权、robots、频率限制。
- [ ] **B2.4 [P1]** 实现匹配策略：文件名清理（去分辨率/编码/组名）、标题+年份匹配、置信度排序。

### B3. HTTP 路由层（bot/api/routes/）

- [ ] **B3.1 [P0]** 新建 `bot/api/routes/emby_metadata.py`，路由前缀 `/api/emby/metadata`：
  - `GET /system-info` 测试连接
  - `GET /movies` 电影列表（分页）
  - `GET /movies/{item_id}` 完整条目
  - `POST /search` 搜索候选
  - `POST /update/{item_id}` 字段级更新（需确认参数，不能仅 GET 触发写入）
- [ ] **B3.2 [P0]** 在 `bot/api/app.py` 注册新 router。
- [ ] **B3.3 [P1]** 统一错误响应格式（Emby 不可达 / API Key 无效 / 数据源超时 / 无结果）。

### B4. 前端（web/，后续阶段）

- [ ] **B4.1** 连接配置区（Emby 地址、API Key、测试连接）。
- [ ] **B4.2** 电影列表区（标题/年份/是否缺元数据/状态）。
- [ ] **B4.3** 搜索与候选展示区。
- [ ] **B4.4** 预览与字段级勾选更新区。
- [ ] **B4.5** 加载/空/成功/失败状态。

### B5. 配置与安全

- [ ] **B5.1 [P0]** Emby API Key 只保存在服务端 `.env`，`.env.example` 仅模板。
- [ ] **B5.2 [P0]** Web 服务默认绑定 `127.0.0.1`；公网用需 VPN/反代/访问控制。
- [ ] **B5.3 [P0]** 不在日志记录 API Key/Cookie/敏感请求头。
- [ ] **B5.4 [P1]** 外部 URL 白名单（仅允许配置的数据源域名），防 SSRF。
- [ ] **B5.5 [P1]** 更新接口必须带确认参数，不能仅 GET 触发写入。

### B6. 测试与验证

- [ ] **B6.1** 正常唯一匹配 → 成功更新。
- [ ] **B6.2** 标题带年份 → 去年后仍匹配。
- [ ] **B6.3** 文件名含分辨率/编码 → 清理后匹配。
- [ ] **B6.4** 空标题 → 提示手动输入。
- [ ] **B6.5** 无结果 → 不修改 Emby。
- [ ] **B6.6** 多候选 → 按置信度排序，人工确认。
- [ ] **B6.7** 已有元数据 → 默认不覆盖。
- [ ] **B6.8** 勾选覆盖 → 只更新勾选字段。
- [ ] **B6.9** API Key 错误 → 连接测试失败并给原因。
- [ ] **B6.10** 图片下载/Emby 更新失败 → 保留错误状态，不显示假成功。

---

## 优先执行顺序

按用户确认，B1（EmbyClient 元数据更新能力补全）已完成，下一优先为 B1.4 真实联调与 A1 清理，其余按以下顺序推进：

1. ~~**B1** EmbyClient 能力补全~~（已完成，待联调）→ **B1.4 真实 Emby 联调**
2. **A1** 清理冗余（低风险，可并行）
3. **B2** 抓取业务层骨架
4. **B3** HTTP 路由层
5. **A3** Emby 重构（高风险，单独立项）
6. **A2 / A4** 配置重命名 + handlers 治理
7. **B4** 前端
8. **A5** 细节一致性

---

## 变更记录

| 日期 | 内容 |
|------|------|
| 2026-07-28 | 初始创建，汇总结构治理 + gv-data-capture 任务 |
