# 🌸 桜色男孩 (LustfulBoy) - 经济系统设计

> **状态**: 📝 设计中
> **日期**: 2025-12-24
> **官网**: lustfulboy.com
> **适用**: Telegram Bot (Aiogram 3.x)
> **主题**: BL / Gay / Emby / 社交

## 1. 核心概念 (Core Concepts)

采用积分制激励用户活跃。

*   **代币名称**: **精粹 (Essence)**
*   **代币符号**: `💧`
*   **核心逻辑**: 
    *   用户通过活跃（签到、聊天、互动）获取代币。
    *   积攒精粹可以换取 Emby 观影特权或群组特殊身份。

## 2. 交互体验设计 (UX Design)

❌ **弃用**: 传统的 `/sign` 命令行交互。
✅ **采用**: 全 **UI/按钮** 驱动，无缝集成在用户面板中。

### A. 每日签到 (Daily Check-in)
*   **入口**: 在【账号中心】面板显示 `[📅 每日签到]` 按钮。
*   **状态显示**: 
    *   未签到时：按钮亮起，文案为 `📅 每日签到`。
    *   已签到时：按钮变灰或文案变为 `✅ 今日已签`，并在面板下方显示 `💧 精粹: 120`。
*   **交互流程**:
    1.  用户点击 `[📅 每日签到]`。
    2.  Bot 弹窗 (Alert) 提示：
        > "签到成功！
        > 获得: +20 💧
        > 连续: 3 天 (加成 +10%)
        >
        > 💬 今日运势: 宜面基，忌独守空房。"
    3.  Bot **静默刷新** 原消息，更新余额显示，不发送新消息刷屏。

### B. 精粹商店 (Essence Store)
*   **入口**: `[🛍️ 精粹商店]` 按钮。
*   **界面**: 使用内联键盘翻页展示可兑换商品。
*   **商品**:
    *   **Emby 时长**: 100 💧 = 1天观看权。
    *   **补签卡**: 50 💧 = 恢复断签。
    *   **特殊头衔**: "🌸 樱之主" / "🐶 忠犬" (群组 Title)。

## 3. 数据库设计 (Schema Optimization)

使用通用的命名规范，便于维护和扩展。所有表均包含基础审计字段 (`created_at`, `updated_at`, `is_deleted` 等)。

### 1. `user_extend` (用户扩展表)
复用已有的 `user_extend` 表，新增经济系统相关字段。

| 字段名 | 类型 | 属性 | 说明 |
| :--- | :--- | :--- | :--- |
| `user_id` | BigInt | PK, FK | 关联 `users.id` |
| ... | ... | ... | (原有字段: role, emby_user_id, bio 等) |
| `balance` | Integer | Default 0 | 当前精粹余额 |
| `total_accumulated` | BigInt | Default 0 | 历史累计获取总量 (用于等级/成就) |
| `streak_days` | Integer | Default 0 | 当前连续签到天数 |
| `last_checkin_date` | Date | Nullable | 上次签到日期 (用于计算断签) |
| `created_at` | DateTime | Auto | 创建时间 |
| `updated_at` | DateTime | Auto | 最后更新时间 |

### 2. `economy_ledgers` (资产流水表)
不可变日志，用于审计和回溯。

| 字段名 | 类型 | 属性 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BigInt | PK, Auto | 流水ID |
| `user_id` | BigInt | FK, Index | 关联 `users.id` |
| `amount` | Integer | Not Null | 变动数值 (正数为获取，负数为消耗) |
| `event_type` | Varchar(32)| Index | 事件类型: `daily_checkin`, `redeem_emby` 等 |
| `meta` | JSON | Nullable | 扩展信息 (如: `{"streak": 5}`) |
| `created_at` | DateTime | Auto | 发生时间 |

## 4. 收支规则配置 (Economy Rules)

无需代码硬编码，建议支持配置文件或数据库配置。

### A. 获取规则 (Gain)

| 行为 | 基础数值 | 频率限制 | 说明 |
| :--- | :--- | :--- | :--- |
| **每日签到** | +10 ~ 20 💧 | 1次/天 | 随机浮动 |
| **连签加成** | +10% / 天 | 上限 +100% | 鼓励连续活跃 |
| **邀请新用户** | +50 💧 | 无限制 | 需被邀请人成功绑定 Emby |

### B. 消耗规则 (Spend)

| 商品/服务 | 价格 | 说明 |
| :--- | :--- | :--- |
| **Emby 1天权限** | 100 💧 | 自动延长有效期 |
| **补签卡** | 50 💧 | 恢复断签状态 |
| **特殊头衔** | 500 💧 | 购买群组自定义 Title |

## 4. 签到算法 (Algorithm)

*   **基础奖励**: 10 ~ 20 💧 (随机浮动，增加趣味性)。
*   **连签加成**: 
    *   `bonus = base * min(streak * 0.1, 1.0)`
    *   上限为 100% 加成 (即连签10天达到双倍)。
*   **断签逻辑**: 
    *   若 `today - last_checkin_date > 1`，则 `streak_days` 重置为 1。

## 5. 开发计划 (Roadmap)

1.  **Phase 1**: 创建数据库表，实现 `EconomyService` (增删查改)。
2.  **Phase 2**: 修改 `get_account_center_keyboard`，加入签到按钮。
3.  **Phase 3**: 实现签到回调逻辑 (`checkin_callback`) 与弹窗反馈。
4.  **Phase 4**: 开发商店界面与兑换逻辑。
